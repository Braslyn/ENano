description = 'Servidor nanohttpd SPA'

jar {
    manifest {
        attributes 'Main-Class': 'com.eif400.server.ENano'
    }
    from {
        configurations.compile.collect{it.isDirectory() ? it : zipTree(it)}
    }
}